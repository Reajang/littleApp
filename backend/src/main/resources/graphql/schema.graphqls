type TestEntity {
    id: ID!,
    createDate: String,
    editDate: String,
    content: String,
}

type Query {
#    testEntities(page: Int, size: Int): [TestEntity]
    testEntity(id: ID): TestEntity
}

#type Mutation {
#    createTestEntity(content: String!): TestEntity
#}
